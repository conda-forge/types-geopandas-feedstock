{% set version = "1.1.4.20260628" %}

package:
  name: types-geopandas
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/t/types-geopandas/types_geopandas-{{ version }}.tar.gz
  sha256: 4b62c384d966f7dcabb0e417d978769a97d1e942779dbcf630f4637476c34169

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}.*
    - setuptools >=77.0.3
    - pip
  run:
    - python >={{ python_min }}
    - types-shapely
    - numpy >=1.20
    - pandas-stubs
    - pyproj

test:
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}.*

about:
  home: https://github.com/python/typeshed
  summary: Typing stubs for geopandas
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - p-vdp
